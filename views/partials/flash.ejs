<% if (success) { %>
  <div class="flash flash--success" role="alert" aria-live="polite"><%= success %></div>
<% } %>
<% if (error) { %>
  <div class="flash flash--error" role="alert" aria-live="assertive"><%= error %></div>
<% } %>
